// Widget for individual joke card

import 'package:flutter/material.dart';

class JokeCard extends StatelessWidget {
  const JokeCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
